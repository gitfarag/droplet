name: deployer
services: 
  frontend:
    build:
      context: ./portfolio
      target: production
      args:
        VITE_API_URL: https://api.${DOMAIN_NAME}
    restart: unless-stopped
    networks:
      - jen-app
      
volumes:
  mysql_data:
    driver: local

networks:
  jen-app:
    driver: bridge